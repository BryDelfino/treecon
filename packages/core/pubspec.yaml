name: core
description: "Core logic and utilities."
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
